// Wrapper de entrada para permitir uso de bundlers (ex: Vite)
// Importa o app monolítico atual que está em assets.
import '../assets/app.js';

// Nota: manteremos o arquivo original em assets/ para migração gradual.
